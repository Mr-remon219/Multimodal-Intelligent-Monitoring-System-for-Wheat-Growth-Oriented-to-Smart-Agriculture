# Generated by Django 5.2.9 on 2026-04-22 00:00

from django.db import migrations


def create_sensor_tables_for_existing_users(apps, schema_editor):
    user_model = apps.get_model("web_page", "User")
    with schema_editor.connection.cursor() as cursor:
        for user_id in user_model.objects.values_list("id", flat=True):
            table_name = f"user_sensor_data_{int(user_id)}"
            quoted_table_name = schema_editor.quote_name(table_name)
            cursor.execute(
                f"""
                CREATE TABLE IF NOT EXISTS {quoted_table_name} (
                    id BIGINT AUTO_INCREMENT PRIMARY KEY,
                    soil_type VARCHAR(20) NOT NULL,
                    seedling_stage VARCHAR(20) NOT NULL,
                    moi DOUBLE NOT NULL,
                    temp DOUBLE NOT NULL,
                    humidity DOUBLE NOT NULL,
                    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
                )
                """
            )


class Migration(migrations.Migration):

    dependencies = [
        ("web_page", "0004_user_sensor_key"),
    ]

    operations = [
        migrations.RunPython(create_sensor_tables_for_existing_users, migrations.RunPython.noop),
    ]
